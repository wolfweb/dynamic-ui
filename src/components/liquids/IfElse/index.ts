import { LiquidElementMetadata } from '@/models/schema';

const meta = new LiquidElementMetadata();

meta.id = "";
meta.key = "IfElse";
meta.display = "IfElse";
meta.attributes = {
  icon: 'if/else',
  condition: ''
};

export default meta;