import { LiquidElementMetadata } from '@/models/schema';

const meta = new LiquidElementMetadata();

meta.id = "";
meta.key = "CardView";
meta.display = "卡片视图";
meta.attributes = {
  icon: 'if/else',
  title: '这里是标题',
  
};

export default meta;